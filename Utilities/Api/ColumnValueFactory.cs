
namespace com.baysideonline.BccMonday.Utilities.Api
{
    public class ColumnValueFactory
    {
        /*protected IBoard _board { get; set; }

        public ColumnValueFactory(IBoard board)
        {
            _board = board;
        }

        public IColumnValue Create(dynamic data)
        {
            if (data == null)
                return null;

            var type = data["type"].Value;
            IColumnValue columnValue;

            switch (type)
            {
                case "color":
                    columnValue = new ColorColumnValue();
                    break;
                case "board-relation":
                    columnValue = new ItemListColumnValue();
                    break;
                case "file":
                    columnValue = new FileColumnValue();
                    break;
                default:
                    columnValue = new ColumnValue();
                    break;
            }

            //columnValue.Build(data, _board);

            return columnValue;
        }*/
    }
}
